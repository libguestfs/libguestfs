#ifndef EVERRUN_UTILS_H
#define EVERRUN_UTILS_H

extern void everrun_trim (char* origin, char *result);

extern void get_everrun_obj_id (char* mixed_id, char *id);

extern void get_everrun_passwd (char *passwd);

#endif
